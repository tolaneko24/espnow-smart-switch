#ifndef PLATFORM_H
#define PLATFORM_H
#include <WiFi.h>
#include <esp_now.h>
// Chọn thư viện WiFi / ESP-NOW / Blynk theo chip (Arduino IDE define ESP8266 hoặc ESP32).

#include <Arduino.h>

/*
#if defined(ESP8266)
    #include <ESP8266WiFi.h>
    #include <espnow.h>
    #define PLATFORM_NAME "ESP8266"
#elif defined(ESP32)
    #include <WiFi.h>
    #include <esp_now.h>
    #define PLATFORM_NAME "ESP32"
#else
    #error "Chỉ hỗ trợ ESP8266 hoặc ESP32"
#endif
*/


#endif
